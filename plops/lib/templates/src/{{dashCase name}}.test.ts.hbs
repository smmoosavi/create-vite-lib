import { {{camelCase name}} } from './{{dashCase name}}';
describe('{{camelCase name}}', () => {
  it('return {{dashCase name}}', () => {
    expect({{camelCase name}}()).toBe('{{dashCase name}}');
  });
});
